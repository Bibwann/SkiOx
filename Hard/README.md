# FIRMWARE XOR (firmware_xor.ino)

Ce firmware simule les valeurs des capteurs et chiffre les données envoyées par Bluetooth et USB.

# FIRMWARE SIMULATION (firmware_simulation.ino)

Ce firmware simule les valeurs des capteurs et envoie les données en clair.

# FIRMWARE (firmware.ino)

Ce firmware utilise le potentiomètre A3 pour simuler le % en oxygène dans le sang. Il n'est pas équipé du chiffrement.